export default function SettingsPage() {
  return (
    <div className="app-page">
      <h1>Operations settings</h1>
      <p>App-local settings only — no shared state with Dashboard or Workspace.</p>
    </div>
  );
}
